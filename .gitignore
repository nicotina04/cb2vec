/target/
/target-ci-*/
/bindings/**/bin/
/bindings/**/obj/

# Release staging: built binaries and the packaged release archive.
/build/

# Files staged into the Unity package from elsewhere in the repo. Only the
# .meta files, the Editor script, the asmdefs, the tests, and the README are
# checked in; binaries come from a build and the binding from bindings/csharp.
/unity/Assets/CB2Vec/Plugins/x86_64/*.dll
/unity/Assets/CB2Vec/Plugins/Android/*/*.so
/unity/Assets/CB2Vec/Runtime/CB2VecNative.cs
